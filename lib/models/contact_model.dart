import 'package:flutter/material.dart';

class ContactModel {
  IconData icon;
  String title;
  String subtitle;

  ContactModel(
      {required this.icon, required this.title, required this.subtitle});
}
